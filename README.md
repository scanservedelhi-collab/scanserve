# Simple Restaurant Demo

A small React demo that loads menu from Supabase and stores orders.

## Local development

1. copy `.env.example` to `.env.local` and fill your Supabase URL and anon key.
2. npm install
3. npm start

## Deploy
This repo uses GitHub Actions to publish to GitHub Pages.
